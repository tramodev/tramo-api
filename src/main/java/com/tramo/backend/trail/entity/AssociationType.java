package com.tramo.backend.trail.entity;

/** The reason a source item is tied to its target. */
public enum AssociationType {
    REQUIRES,
    ELABORATES,
    CONTRADICTS,
    EXAMPLE_OF,
    RELATED
}
